kind: pipeline
type: docker
name: drone-test

steps:
- name: Drone-Scan-Trigger
  image: apisec/drone-scan:latest
  pull: always
  commands: 
    - docker run --name test -e "host=https://developer.apisec.ai"   -e "username=mohd@apisec.ai" -e "password=<password>" -e "projectName=<projectname>" -e "profile=Master" -e "scanner=Super_1" -e "email=true" -e "report=RUN_SUMMARY" apisec/drone-scan:latest
  settings:
     host: https://developer.apisec.ai
     username: root
     password: 
     project: HMAC
     profile: Master
     scanner: Super_1
     emailReport: true
     reportType: RUN_SUMMARY
     password:
      from_secret: password
    username:
      from_secret: username

     

 
